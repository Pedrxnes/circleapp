{
  "name": "circle-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Floating circular Claude usage monitor for Windows",
  "author": "Circle <circle@users.noreply.github.com>",
  "homepage": "https://github.com/Pedrxnes/circle",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build && node scripts/generate-icon.cjs",
    "dev": "npm run build && electron .",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "test": "node --test dist/test/*.test.js",
    "check": "npm run typecheck && npm run build && npm run test",
    "package": "npm run build && electron-builder --config electron-builder.yml --publish never",
    "capture:docs": "npm run build && electron scripts/capture-docs.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "electron": "^44.3.0",
    "electron-builder": "^26.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
